# Brave Search API key - Required
# Get your API key from https://api-dashboard.search.brave.com/app/keys
BRAVE_API_KEY=your_brave_api_key_here

# Transport type: 'sse' for Server-Sent Events, or leave empty for stdio
TRANSPORT=sse

# Port for the SSE server (only used when TRANSPORT=sse)
PORT=3000